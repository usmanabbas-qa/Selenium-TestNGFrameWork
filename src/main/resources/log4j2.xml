<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN">
    <Appenders>
        <!-- Console Appender: Logs messages to the console -->
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="%d{HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n"/>
        </Console>
        <!-- File Appender: Logs messages to a file -->
        <File name="File" fileName="logs/automation.log">
            <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n"/>
        </File>
    </Appenders>
    <Loggers>
        <!-- Root Logger: Catches all messages and sends them to defined appenders -->
        <Root level="info"> <!-- Set the default logging level to INFO -->
            <AppenderRef ref="Console"/>
            <AppenderRef ref="File"/>
        </Root>
        <!-- Specific Logger for Selenium and WebDriverManager to reduce their log verbosity -->
        <Logger name="org.openqa.selenium" level="warn"/>
        <Logger name="io.github.bonigarcia" level="warn"/>
    </Loggers>
</Configuration>